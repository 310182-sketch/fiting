# fiting

本專案是一個本地優先的健身紀錄 PWA，使用 React、TypeScript、Vite、Tailwind CSS 與 Dexie 實作，支援模板訓練、熱身／正式組區分、本地 IndexedDB 儲存與簡單的每週統計。

## 開發

```bash
npm install
npm run dev
```

## 建置

```bash
npm run build
npm run preview
```

瀏覽器中開啟顯示的本機網址即可使用（支援安裝為 PWA）。
